"use client";

/**
 * Active order tracking is now handled cleanly by BottomActionBar,
 * keeping the screen uncluttered and avoiding overlap with MusicPlayer.
 */
export default function ActiveOrderWidget() {
  return null;
}
